[project]
name = "meeting-env"
version = "0.1.0"
description = "Meeting Optimization Environment for Hackathon"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []